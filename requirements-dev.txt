# Dev dependencies mirroring pyproject.toml [project.optional-dependencies] dev.
# CI installs via `pip install -e '.[dev]'`; this file is a convenience pin for
# reproducible local environments. Keep in sync with pyproject's [dev] extra.
ruff>=0.5
mypy>=1.10
pytest>=8.2
pytest-cov>=5.0
respx>=0.21
testcontainers>=4.0
boto3-stubs[cloudwatch,logs,sts,sns]>=1.34
types-PyYAML>=6.0
yamllint>=1.35
