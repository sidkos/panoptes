# Panoptes v0.1 — Next-Session Implementation Resume Prompt

Paste the block below to start the implementation session. Planning is complete
and `fb`-ready; this session builds v0.1.

---

## Prompt

> Implement **Panoptes v0.1** by driving `fb` against the finalized spec +
> implementation playbook. This is the standalone repo at
> **`/Users/sergey/repos/gitHub/panoptes`** (also symlinked at
> `/Users/sergey/repos/gitHub/fida/panoptes`). Work ONLY in the panoptes repo —
> never touch FIDA. Panoptes is a PUBLIC OSS repo: keep it **brand-neutral**
> (zero literal "fida"/"FIDA" — verify with `grep -rin fida` → 0).
>
> **Read first (authoritative, finalized):**
> - `docs/specs/v0.1_implementation_plan.md` — the build playbook (phase→files→tests, risk register R1–R14, Definition-of-Ready). `fb` executes this.
> - `docs/specs/v0.1_core_local_proof.md` — the spec (Tests, DoD, Directory Layout, CI/CD).
> - `docs/{ARCHITECTURE,ADAPTERS,DASHBOARDS,IAM,ROADMAP}.md` + `README.md` — design context.
>
> **Locked — do NOT re-litigate:** standalone/plugin core-vs-consumer boundary
> (structurally enforced: `core/` must not import `examples/`); two dashboard
> tiers + deploy-time injection (core packs ship here; consumer packs injected
> via mounted dir locally / pinned commit-SHA git ref hosted); two-faces-one-store
> (Grafana + MCP read VictoriaMetrics); `env` first-class label; MCP **fully
> read-only** (no write path); single read-only IAM domain; local=docker-compose,
> deploy=Terraform (v0.2); stdio MCP for v0.1 (HTTP+SSO is v0.2); standalone CI in
> this repo; all 7 open questions ACCEPTED; root-cause tracing + MCP
> dashboard-authoring DROPPED.
>
> **Build order (TDD-first, each phase ends green + a semantic commit):**
> Phase 0 scaffold+CI → 1 model/registry/config → 2 stores → 3 sources → 4
> collector+notifier → 5 grafana+packs+injection → 6 MCP stdio → 7 demo pack
> (extension-path proof + brand-free core-purity guard) → 8 compose+integration →
> 9 docs. Phases 2/3 parallelizable; 5 independent of 2–4.
>
> **Per-phase gate:** ruff + ruff format --check + `mypy --strict` +
> `pytest -m "not integration"` + both boundary guards green, then commit.
>
> Launch: `fb` on `docs/specs/v0.1_implementation_plan.md` (the spec is the
> contract; the playbook is the execution detail).

---

## Owner-supplied prerequisites (only block the FINAL manual smoke run, not any automated phase)

Provide these in `examples/demo-pack/.env` before the Phase-8 live `docker-compose up` smoke (CI + all unit/integration phases are hermetic and need none of them):

- A **read-only** AWS profile or assume-role for the reference `dev` environment (CloudWatch + Logs read).
- A **Sentry read** API token + org/project.
- A reachable `DEV_HEALTH_URL` (the game-server `/health` endpoint).
- Local Docker (for VictoriaMetrics + Grafana).

`.env` is gitignored and never committed; the Sentry token must be read-scope only.

## Pre-flight

- The panoptes docs/spec/plan are committed to this repo (done in the planning session) — `fb`'s worktree can see them.
- If `fb`'s worktree lands on a stale SHA, recover with `git fetch && git checkout -B <branch> origin/main` from the worktree.
- After each phase, review the build-agent diff (`git diff --stat`) for scope creep before committing.
